import React, { Component } from 'react';
import { RkText } from 'react-native-ui-kitten';

class ProductDetail extends Component {
    render() {
        return <RkText rkType="danger">Hello from Product Detail</RkText>;
    }
}

export default ProductDetail;